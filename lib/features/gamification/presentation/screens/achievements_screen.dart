import "package:flutter/material.dart";
class AchievementsScreen extends StatelessWidget {
  const AchievementsScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text("Achievements")), body: const Center(child: Text("Gamification - coins/level not computed locally")));
}
